#include <iostream>

using std::cin;
using std::cout;
using std::ios;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

    int a, b;

    cin >> a >> b;
    cout << a + b;

    return 0;
}
